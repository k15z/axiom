test_public_api_is_documented:
  tags: [code-quality]
  on:
    - internal/**/*.go
  condition: >
    All exported functions and types in the core packages should have
    documentation comments. Test files are excluded. Trivial accessors
    or one-line wrappers may be excepted, but anything that forms the
    public API of a package should be documented.
